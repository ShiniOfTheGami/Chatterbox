function Chatterbox(rootApp) {
  this.rootApp = rootApp;
}

Chatterbox.prototype.bootServer = function() {
  // Keep a copy of the active class
  var self    = this,
      rootApp = self.rootApp;
};

Chatterbox.prototype.stopServer = function() {
  // body...
};